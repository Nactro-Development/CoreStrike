<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CoreStrike.DashBord.DashBord"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CoreStrike.DashBord"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid  Padding="20">

        <!-- ROWS -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Top cards -->
            <RowDefinition Height="*"/>
            <!-- Bottom section -->
        </Grid.RowDefinitions>

        <!-- ================= TOP 3 CARDS ================= -->
        <Grid Grid.Row="0" ColumnSpacing="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- CPU CARD -->
            <Border Grid.Column="0" CornerRadius="12"  Background="{ThemeResource LayerFillColorDefaultBrush }" Padding="16" >
                <StackPanel Spacing="3">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Foreground="Gray" Glyph="&#xE950;" FontSize="16"/>
                        <TextBlock Foreground="Gray" Text="CPU" FontWeight="SemiBold"/>
                    </StackPanel>

                    <TextBlock Text="{x:Bind CpuName, Mode=OneWay}" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap"/>

                    <Border Height="80" 
                CornerRadius="4" 
                Background="#1A000000" 
                Margin="0,12,0,12">
                        
                        
                        <lvc:CartesianChart Background="Transparent" x:Name="CpuChart"
                                Series="{x:Bind CpuSeries}"
                                XAxes="{x:Bind XAxes}"
                                YAxes="{x:Bind YAxes}"/>
                    </Border>
                    
                    <TextBlock Text="{x:Bind CpuSpeed, Mode=OneWay}" FontSize="28" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="{x:Bind CpuTemperature, Mode=OneWay}" FontSize="18" Foreground="#FF6B5B"/>
                    <TextBlock Text="{x:Bind CpuDisplayText, Mode=OneWay}" Foreground="#94A3B8"/>
                </StackPanel>
            </Border>

            <!-- GPU CARD -->
            <Border Grid.Column="1" CornerRadius="12" Background="{ThemeResource LayerFillColorDefaultBrush }" Padding="16" >
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Foreground="Gray" Glyph="&#xE7F4;" FontSize="16"/>
                        <TextBlock Foreground="Gray" Text="GPU" FontWeight="SemiBold"/>
                    </StackPanel>
                    
                    <TextBlock Text="12.4 GB VRAM" FontSize="28" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="78°C | 2150 MHz" Foreground="#94A3B8"/>
                </StackPanel>
            </Border>

            <!-- RAM CARD -->
            <Border Grid.Column="2" CornerRadius="12" Background="{ThemeResource LayerFillColorDefaultBrush }" Padding="16" >
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Foreground="Gray" Glyph="&#xEEA0;" FontSize="16"/>
                        <TextBlock Foreground="Gray" Text="RAM" FontWeight="SemiBold"/>
                    </StackPanel>
                    <TextBlock Text="64 GB" FontSize="28" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="24.2 GB Used" Foreground="#94A3B8"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- ================= BOTTOM SECTION ================= -->
        <Grid Grid.Row="1" Margin="0,20,0,0" ColumnSpacing="20">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <!-- Big storage -->
                <ColumnDefinition Width="1*"/>
                <!-- Right side -->
            </Grid.ColumnDefinitions>

            <!-- LARGE STORAGE CARD -->
            <Border Grid.Column="0" CornerRadius="12" Background="{ThemeResource LayerFillColorDefaultBrush }" Padding="20" >
                <StackPanel Spacing="10">
                    <TextBlock Text="NVMe Gen5 Performance" Foreground="Gray"/>
                    <TextBlock Text="Read: 12400 MB/s" FontSize="24" Foreground="White"/>
                    <TextBlock Text="Write: 10800 MB/s" FontSize="24" Foreground="White"/>
                    <TextBlock Text="Capacity: 2 TB" Foreground="#94A3B8"/>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Deep Scan"/>
                        <Button Content="Optimize"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- RIGHT SIDE STACK -->
            <Grid Grid.Column="1" RowSpacing="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- NETWORK -->
                <Border Grid.Row="0" CornerRadius="12" Background="{ThemeResource LayerFillColorDefaultBrush }" Padding="16" >
                    <StackPanel>
                        <TextBlock Text="Network" Foreground="Gray"/>
                        <TextBlock Text="Download: 842 Mbps" Foreground="White"/>
                        <TextBlock Text="Upload: 120 Mbps" Foreground="White"/>
                        <TextBlock Text="Latency: 9 ms" Foreground="#94A3B8"/>
                    </StackPanel>
                </Border>

                <!-- PROCESSES -->
                <Border Grid.Row="1" CornerRadius="12" Background="{ThemeResource LayerFillColorDefaultBrush }" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Processes" Foreground="Gray"/>
                        <TextBlock Text="System_Kernel.bin - 1.2%" Foreground="White"/>
                        <TextBlock Text="Kinetic_Engine.exe - 8.4%" Foreground="White"/>
                        <TextBlock Text="Graphics_Renderer - 14.2%" Foreground="White"/>
                    </StackPanel>
                </Border>

            </Grid>
        </Grid>

    </Grid>
    
</Page>

